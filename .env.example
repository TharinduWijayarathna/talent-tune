APP_NAME=TalentTune
APP_ENV=local
APP_KEY=base64:5hd08SLdFUNn+MmVoRABQx2ZI/UlmWA/N1XnRSPctX0=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Base domain for institution subdomains (e.g. institution-slug.{APP_DOMAIN})
# Local: talenttune.test   Production: talenttune.com
APP_DOMAIN=talenttune.test
# Subdomains that are not institution slugs (comma-separated)
APP_RESERVED_SUBDOMAINS=www,app,talenttune
# TLD that indicates local dev; two-part host (acme.{APP_LOCAL_TLD}) is treated as subdomain
APP_LOCAL_TLD=.test

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

GOOGLE_TTS_API_KEY=

GEMINI_API_KEY=

# Stripe (sandbox: use test keys pk_test_*, sk_test_*; create a Product + monthly Price in Dashboard)
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_PRICE_ID=          # Recurring monthly price ID (price_xxx)
STRIPE_CURRENCY=usd

# Dockploy/Vintorr Panel API – create subdomains when institutions are activated (production)
DOCKPLOY_TOKEN=
DOCKPLOY_APPLICATION_ID=
# DOCKPLOY_API_URL=https://panel.vintorr.com
